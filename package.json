{
  "name": "snark-artifacts-root",
  "private": true,
  "scripts": {
    "changeset": "changeset"
  },
  "devDependencies": {
    "@changesets/cli": "^2.27.1"
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "sparse-checkout": "./scripts/bin/setup",
    "gprf": "git pull-rebase-filtered"
  }
}
